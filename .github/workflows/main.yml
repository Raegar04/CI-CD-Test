name: Copy-to-specificaions

on:
  push:
    branches:
      - main

jobs:
  copy_file:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Copy file to specifications
        run: cp CI-CD-Test/test.txt Specifications/
